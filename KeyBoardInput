package com.company;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {

// This version of this class will be for DEMO 1 and will be further updated with JAVA FX for future use

        // Methods

        String basic = "";

        System.out.println("Enter desired direction of movement...(case doesn't matter)\nW - Jump\nA - Move Left\nD - Move Right\nP - Dash");

        Scanner keyboard = new Scanner(System.in);
        String input = keyboard.next();

        // System.out.println("S - Crouch"); still not decided if this will be implemented in the game

        while(input.equalsIgnoreCase("w") || input.equalsIgnoreCase("a") || input.equalsIgnoreCase("d") || input.equalsIgnoreCase("p")){
            System.out.println("Enter desired direction of movement...(case doesn't matter)\nW - Jump\nA - Move Left\nD - Move Right\nP - Dash");

            input = keyboard.next();
        }
    }

    // Move Right

    // Move Left

    // Dash Right

    // Dash Left

    // Move Up (Jump)

    // Crouch (This may or may not be of use in the final game)


}


